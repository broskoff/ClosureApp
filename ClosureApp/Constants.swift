import UIKit

enum Constants {
	
	enum Text {
		static let hello = "Hello"
		static let buttonTitle = "Пуньк"
		static let placeholder = "Введите текст"
		static let transferTextButtonTitle = "Передать текст"
		static let noData = "no data"
	}
	
	enum Layout {
		static let cornerRadius: CGFloat = 8
		
		static let verticalSpacing: CGFloat = 20
		
		static let labelWidthMultiplier: CGFloat = 0.5
		static let labelHeightMultiplier: CGFloat = 0.3
		
		static let buttonWidthMultiplier: CGFloat = 0.3
		static let buttonHeightMultiplier: CGFloat = 0.5
		
		static let textFieldWidthMultiplier: CGFloat = 0.5
		static let textFieldHeightMultiplier: CGFloat = 0.3
		
		static let secondButtonWidthMultiplier: CGFloat = 0.5
		static let secondButtonHeightMultiplier: CGFloat = 0.3
		
		static let keyboardOffset: CGFloat = -150
		static let defaultViewOriginY: CGFloat = 0
	}
	
	enum Font {
		static let buttonSize: CGFloat = 20
	}
}
